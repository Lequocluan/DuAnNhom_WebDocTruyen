import SectionChapter from "../components/section_chapter/SectionChapter";

function Chapter() {
  // useEffect(() => {
  //   document.title = `Truyện online.vn - ${slugStory} - Chương ${slugChapter}`; // Cập nhật document.title
  // }, [slugStory, slugChapter]);

  return (
    <>
      <SectionChapter />
    </>
  );
}

export default Chapter;
